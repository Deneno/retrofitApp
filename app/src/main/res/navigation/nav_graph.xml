<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_grahp"
    app:startDestination="@id/splashFragment">

    <fragment
        android:id="@+id/rootFragment"
        android:name="ya.deneno.retrofitapp.screens.root.RootFragment"
        android:label="fragment_root"
        tools:layout="@layout/fragment_root" />
    <fragment
        android:id="@+id/splashFragment"
        android:name="ya.deneno.retrofitapp.screens.splash.SplashFragment"
        android:label="fragment_splash"
        tools:layout="@layout/fragment_splash" >
        <action
            android:id="@+id/action_splashFragment_to_rootFragment"
            app:destination="@id/rootFragment" />
    </fragment>
    <fragment
        android:id="@+id/cashlessFragment"
        android:name="ya.deneno.retrofitapp.screens.cashless.CashlessFragment"
        android:label="fragment_cashless"
        tools:layout="@layout/fragment_cash" />
    <fragment
        android:id="@+id/cashFragment2"
        android:name="ya.deneno.retrofitapp.screens.cash.CashFragment"
        android:label="fragment_cash"
        tools:layout="@layout/fragment_cash" />
</navigation>